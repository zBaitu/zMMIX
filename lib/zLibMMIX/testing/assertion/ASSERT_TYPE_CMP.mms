    PREFIX :testing:assertion:
ASSERT_BYTE_EQ  SET assert:data_type,BYTE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,EQ
                JMP assert

ASSERT_BYTE_EQU SET assert:data_type,BYTE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,EQ
                JMP assert

ASSERT_BYTE_GE  SET assert:data_type,BYTE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,GE
                JMP assert

ASSERT_BYTE_GEU SET assert:data_type,BYTE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,GE
                JMP assert

ASSERT_BYTE_GT  SET assert:data_type,BYTE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,GT
                JMP assert

ASSERT_BYTE_GTU SET assert:data_type,BYTE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,GT
                JMP assert

ASSERT_BYTE_LE  SET assert:data_type,BYTE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,LE
                JMP assert

ASSERT_BYTE_LEU SET assert:data_type,BYTE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,LE
                JMP assert

ASSERT_BYTE_LT  SET assert:data_type,BYTE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,LT
                JMP assert

ASSERT_BYTE_LTU SET assert:data_type,BYTE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,LT
                JMP assert

ASSERT_BYTE_NE  SET assert:data_type,BYTE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,NE
                JMP assert

ASSERT_BYTE_NEU SET assert:data_type,BYTE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,NE
                JMP assert

ASSERT_WYDE_EQ  SET assert:data_type,WYDE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,EQ
                JMP assert

ASSERT_WYDE_EQU SET assert:data_type,WYDE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,EQ
                JMP assert

ASSERT_WYDE_GE  SET assert:data_type,WYDE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,GE
                JMP assert

ASSERT_WYDE_GEU SET assert:data_type,WYDE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,GE
                JMP assert

ASSERT_WYDE_GT  SET assert:data_type,WYDE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,GT
                JMP assert

ASSERT_WYDE_GTU SET assert:data_type,WYDE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,GT
                JMP assert

ASSERT_WYDE_LE  SET assert:data_type,WYDE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,LE
                JMP assert

ASSERT_WYDE_LEU SET assert:data_type,WYDE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,LE
                JMP assert

ASSERT_WYDE_LT  SET assert:data_type,WYDE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,LT
                JMP assert

ASSERT_WYDE_LTU SET assert:data_type,WYDE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,LT
                JMP assert

ASSERT_WYDE_NE  SET assert:data_type,WYDE_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,NE
                JMP assert

ASSERT_WYDE_NEU SET assert:data_type,WYDE_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,NE
                JMP assert

ASSERT_TETRA_EQ SET assert:data_type,TETRA_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,EQ
                JMP assert

ASSERT_TETRA_EQU    SET assert:data_type,TETRA_TYPE
                    SET assert:sign,UNSIGNED
                    SET assert:comparer,EQ
                    JMP assert

ASSERT_TETRA_GE     SET assert:data_type,TETRA_TYPE
                    SET assert:sign,SIGNED
                    SET assert:comparer,GE
                    JMP assert

ASSERT_TETRA_GEU    SET assert:data_type,TETRA_TYPE
                    SET assert:sign,UNSIGNED
                    SET assert:comparer,GE
                    JMP assert

ASSERT_TETRA_GT     SET assert:data_type,TETRA_TYPE
                    SET assert:sign,SIGNED
                    SET assert:comparer,GT
                    JMP assert

ASSERT_TETRA_GTU    SET assert:data_type,TETRA_TYPE
                    SET assert:sign,UNSIGNED
                    SET assert:comparer,GT
                    JMP assert

ASSERT_TETRA_LE     SET assert:data_type,TETRA_TYPE
                    SET assert:sign,SIGNED
                    SET assert:comparer,LE
                    JMP assert

ASSERT_TETRA_LEU    SET assert:data_type,TETRA_TYPE
                    SET assert:sign,UNSIGNED
                    SET assert:comparer,LE
                    JMP assert

ASSERT_TETRA_LT     SET assert:data_type,TETRA_TYPE
                    SET assert:sign,SIGNED
                    SET assert:comparer,LT
                    JMP assert

ASSERT_TETRA_LTU    SET assert:data_type,TETRA_TYPE
                    SET assert:sign,UNSIGNED
                    SET assert:comparer,LT
                    JMP assert

ASSERT_TETRA_NE     SET assert:data_type,TETRA_TYPE
                    SET assert:sign,SIGNED
                    SET assert:comparer,NE
                    JMP assert

ASSERT_TETRA_NEU    SET assert:data_type,TETRA_TYPE
                    SET assert:sign,UNSIGNED
                    SET assert:comparer,NE
                    JMP assert

ASSERT_OCTA_EQ  SET assert:data_type,OCTA_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,EQ
                JMP assert

ASSERT_OCTA_EQU SET assert:data_type,OCTA_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,EQ
                JMP assert

ASSERT_OCTA_GE  SET assert:data_type,OCTA_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,GE
                JMP assert

ASSERT_OCTA_GEU SET assert:data_type,OCTA_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,GE
                JMP assert

ASSERT_OCTA_GT  SET assert:data_type,OCTA_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,GT
                JMP assert

ASSERT_OCTA_GTU SET assert:data_type,OCTA_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,GT
                JMP assert

ASSERT_OCTA_LE  SET assert:data_type,OCTA_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,LE
                JMP assert

ASSERT_OCTA_LEU SET assert:data_type,OCTA_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,LE
                JMP assert

ASSERT_OCTA_LT  SET assert:data_type,OCTA_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,LT
                JMP assert

ASSERT_OCTA_LTU SET assert:data_type,OCTA_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,LT
                JMP assert

ASSERT_OCTA_NE  SET assert:data_type,OCTA_TYPE
                SET assert:sign,SIGNED
                SET assert:comparer,NE
                JMP assert

ASSERT_OCTA_NEU SET assert:data_type,OCTA_TYPE
                SET assert:sign,UNSIGNED
                SET assert:comparer,NE
                JMP assert
    PREFIX :
